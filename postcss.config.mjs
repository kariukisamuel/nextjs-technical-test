// This comment provides type-checking and IntelliSense for PostCSS config
/** @type {import('postcss-load-config').Config} */

const config = {
  // Configuration for PostCSS plugins
  plugins: {
    // Tailwind CSS plugin for processing your styles using the Tailwind framework
    tailwindcss: {}, // This initializes the Tailwind CSS plugin
  },
};

export default config; // Export the configuration object for PostCSS to use
